# Auto-generated OpenAPI Axios client (see CLAUDE.md) — committed but never hand-edited,
# regenerated via `npm run api:generate`. Excluded from analysis entirely so its inherent
# per-method boilerplate doesn't count against the duplication quality gate.
sonar.exclusions=src/server/api/generated/**
